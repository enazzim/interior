spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/interior?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: '1111'
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8085
